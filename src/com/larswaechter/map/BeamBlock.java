package com.larswaechter.map;

public class BeamBlock extends AbstractBlock {
    BeamBlock(float x, float y, int mapIdxX, int mapIdxY) {
        super(x, y, mapIdxX, mapIdxY);
        this.setColor(0xFF4c8dd6);
    }
}
